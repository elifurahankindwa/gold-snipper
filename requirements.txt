# ==============================================================
# Gold Sniper v4 — XAUUSD Intelligence Platform
# requirements.txt
#
# Python: 3.10 · 3.11 · 3.12  (3.12 recommended)
#
# Install (standard):
#   pip install -r requirements.txt
#
# Install (Ubuntu/Debian system Python):
#   pip install -r requirements.txt --break-system-packages
#
# Install (virtual environment — recommended for production):
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# All versions pinned to those verified in production.
# ==============================================================


# ── Web Framework ─────────────────────────────────────────────────────────────
Flask==3.1.3
Flask-SocketIO==5.6.1
Werkzeug==3.1.7
Jinja2==3.1.6
click==8.3.1
itsdangerous==2.2.0
MarkupSafe==3.0.3


# ── WebSocket / Real-time transport ───────────────────────────────────────────
python-socketio==5.16.1
python-engineio==4.13.1
simple-websocket==1.1.0

# Async worker — gevent recommended (eventlet is deprecated)
gevent==26.4.0
# eventlet==0.41.0       # fallback if gevent has issues on your platform

# gevent / async dependencies
greenlet==3.3.2
dnspython==2.8.0
bidict==0.23.1


# ── Market Data (Yahoo Finance) ────────────────────────────────────────────────
yfinance==1.3.0
multitasking==0.0.12
peewee==4.0.4
curl_cffi==0.15.0
lxml==6.0.2
beautifulsoup4==4.14.3


# ── Data Science ──────────────────────────────────────────────────────────────
numpy==2.4.3
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0
joblib==1.5.3


# ── Machine Learning ──────────────────────────────────────────────────────────
xgboost==3.2.0

# SHAP explainability (feature importance / trade reasoning)
shap==0.51.0
slicer==0.0.8

# numba required by SHAP for fast tree computation
numba==0.65.0
llvmlite==0.47.0


# ── Chart generation (backend PNG charts for backtest) ────────────────────────
matplotlib==3.10.8
contourpy==1.3.3
cycler==0.12.1
fonttools==4.62.1
kiwisolver==1.5.0
pyparsing==3.3.2


# ── Image processing (chart image upload analysis) ────────────────────────────
Pillow==12.1.1


# ── HTTP / Networking ─────────────────────────────────────────────────────────
requests==2.33.0
urllib3==2.6.3
certifi==2026.2.25
charset-normalizer==3.4.6
idna==3.11
cffi==2.0.0
pycparser==3.0


# ── Date / Time utilities ─────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
pytz==2026.1.post1
six==1.17.0


# ── General utilities ─────────────────────────────────────────────────────────
tqdm==4.67.3
packaging==26.0
platformdirs==4.9.4


# ==============================================================
# OPTIONAL — uncomment as needed
# ==============================================================

# Production WSGI server (recommended over Flask's built-in server)
# gunicorn==23.0.0

# Faster JSON serialisation (drop-in, no code changes needed)
# orjson==3.10.18

# Environment variable management (.env file support)
# python-dotenv==1.0.1

# Task scheduling (alternative to the threading-based live loop)
# APScheduler==3.11.0

# OpenCV — for advanced chart computer vision (not required by default)
# opencv-python-headless==4.10.0.84
